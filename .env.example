# Example environment variables for the banking forecast project

# Database connection string (if using a database)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# MLflow tracking URI; use local directory or remote server
MLFLOW_TRACKING_URI=http://localhost:5000

# S3 bucket for DVC remote (if applicable)
DVC_REMOTE_URL=s3://my-dvc-bucket/data

# Prefect API URL
PREFECT_API_URL=http://localhost:4200

# FastAPI settings
API_HOST=0.0.0.0
API_PORT=8000

# Slack webhook for alerts (optional)
SLACK_WEBHOOK_URL=